import { ModalAdhocDirective } from './modal-adhoc.directive';

describe('ModalAdhocDirective', () => {
  it('should create an instance', () => {
    const directive = new ModalAdhocDirective();
    expect(directive).toBeTruthy();
  });
});
